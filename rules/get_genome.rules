rule get_genome:
    params:
        link=config["genomelink"],
        download=config["genomelink"].split("/")[-1]
    output:
        config["genome"]
    shell:
        "wget -P data/genome {params.link} ; "
        "unzip -d data/genome data/genome/{params.download}"
